import React from 'react';
import pure from 'recompose/pure';

const About = (props) => {
  return (
    <div>
      this is a about page

    </div>
  );
};

export default pure(About);
