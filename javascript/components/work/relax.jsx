import React from 'react';
import pure from 'recompose/pure';

const Relax = (props) => {
  return (
    <div>
      this is a project page
    </div>
  );
};

export default pure(Relax);
