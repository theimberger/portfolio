import React from 'react';
import pure from 'recompose/pure';

const ShiftFour = (props) => {
  return (
    <div>
      this is a project page
    </div>
  );
};

export default pure(ShiftFour);
