import React from 'react';
import pure from 'recompose/pure';

const WikiCrawler = (props) => {
  return (
    <div>
      this is a project page
    </div>
  );
};

export default pure(WikiCrawler);
